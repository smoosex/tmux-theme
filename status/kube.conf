# vim:set ft=tmux:
%hidden MODULE_NAME="kube"

# Requires https://github.com/jonmosco/kube-tmux

set -ogq "@theme_${MODULE_NAME}_icon" "󱃾 "
set -ogqF "@theme_${MODULE_NAME}_color" "#{E:@thm_blue}"
set -ogqF "@theme_kube_context_color" "#{E:@thm_red}"
set -ogqF "@theme_kube_namespace_color" "#{E:@thm_sky}"
set -ogq "@theme_${MODULE_NAME}_text" \
   " #{l:#[fg=#{@theme_kube_context_color}]#{kubectx_context}#[fg=default]:#[fg=#{@theme_kube_namespace_color}]#{kubectx_namespace}}"

source -F "#{d:current_file}/../utils/status_module.conf"
