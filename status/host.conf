# vim:set ft=tmux:
%hidden MODULE_NAME="host"

set -ogq "@theme_${MODULE_NAME}_icon" "󰒋 "
set -ogqF "@theme_${MODULE_NAME}_color" "#{E:@thm_mauve}"
set -ogq "@theme_${MODULE_NAME}_text" " #H"

source -F "#{d:current_file}/../utils/status_module.conf"
